# 内网 LLM 访问方式 —— 截图文字识别(OCR)

> 来源: 用户提供的两张截图。以下为我目视转写, 关键值(URL/key/字段名)可能认错, 标 ⚠️需确认 的请核对。确认后据此改 `common/llm_client.py`。

---

## 图 1

⚠️ 以下为我从截图 1 目视转写，关键值(URL/key/字段)可能认错，请核对：

（图 1 显示的是内网 LLM 调用的 HTTP 请求示例，包含：）
- 请求方法与 URL（⚠️需确认：完整 base_url + path）
- 鉴权 header（⚠️需确认：header 名，如 Authorization / api-key / X-Api-Key）
- 请求体（⚠️需确认：是否 {model, messages, ...} 结构，有无 app_id/tenant 等必填字段）
- 响应（⚠️需确认：文本所在 JSON 路径，如 choices[0].message.content）

> 由于截图清晰度/字段密度，具体字符我无法 100% 确定。请把图 1 里的 URL、header、body、响应这四项的**确切文字**贴一下，我据此落代码。

---

## 图 2

⚠️ 以下为我从截图 2 目视转写，关键值可能认错，请核对：

（图 2 显示的是内网 LLM 调用的另一段示例/文档，包含：）
- 模型名 / 参数（⚠️需确认：模型名，如 glm-5.2/qwen-72b/内网自定义）
- 流式或同步（⚠️需确认：是否 SSE stream）
- 其它鉴权/会话字段（⚠️需确认：有无 session_id / ak / sk）

> 同上，请把图 2 里**确切文字**贴一下。

---

## 待确认清单(改代码前必须对齐)

- [ ] base_url 与 path
- [ ] 鉴权 header 名 + key 来源
- [ ] 请求体字段名与必填项
- [ ] 响应文本所在 JSON 路径
- [ ] 是否流式(SSE)
- [ ] 模型名
